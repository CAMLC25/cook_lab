<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardBackgroundColor="#FF2E2E2E"
    app:cardElevation="4dp">

    <!-- Use ConstraintLayout to prevent TextViews from overlapping -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Text Container for Search Term and Time -->
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_marginStart="8dp"
            app:layout_constraintEnd_toStartOf="@+id/clearSearchHistory"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Search Term Text -->
            <TextView
                android:id="@+id/searchTermText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Search Term"
                android:textColor="@android:color/white"
                android:textSize="16sp"
                android:ellipsize="end"
                android:maxLines="1" />

            <!-- Time Text -->
            <TextView
                android:id="@+id/searchTimeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Ngày hôm nay"
                android:textColor="#B3B3B3"
                android:textSize="12sp"
                android:layout_marginTop="4dp" />
        </LinearLayout>

        <!-- Clear Button (X icon) -->
        <ImageView
            android:id="@+id/clearSearchHistory"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginStart="auto"
            android:src="@drawable/ic_cancel_white"
            app:tint="@android:color/white"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
